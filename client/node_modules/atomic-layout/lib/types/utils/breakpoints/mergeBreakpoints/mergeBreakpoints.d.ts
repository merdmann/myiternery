import { AreaBreakpoint } from '../getAreaBreakpoints';
/**
 * Merges two given mergable breakpoints.
 */
export default function mergeBreakpoints(breakpointA: AreaBreakpoint, breakpointB: AreaBreakpoint, includesArea: boolean): AreaBreakpoint;
