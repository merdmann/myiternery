import { Props } from "../../strings/parsePropName";
import { AreasMap } from '../generateComponents';
declare type ParseTemplates = (props: Props) => AreasMap;
declare const parseTemplates: ParseTemplates;
export default parseTemplates;
